# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

### Added

- Initial implementation of `Result` and `Result<T>` types
- `Try`, `Map`, `Bind`, and `Match` methods
- Fluent pipeline support
- Exception-safe operations with `Result.Try`
- implementation of `Error` record.
- Unit test codes (**`ResultTests`**, **`ResolOfTTests`**) added.
- **`docs.yml`** file added to gothub workflows for **`github pages`**. 

### Changed

- Changed project inside folder structure. 
- Refactored core code for more clean.
- edited **`.Core`** namespace.
- edited readme file.

### Fixed

- fixed **`docs.yml`** **`--force`** keyword on **`Build Documentation`** step.








[unreleased]: https://github.com/ferdikurnazdm/Resultify

